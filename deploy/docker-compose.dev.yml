version: '3.9'

volumes:
  conf-vol:
    name: ${CONF_VOL_NAME}
    driver: local
    driver_opts:
      type: ${VOL_TYPE}
      o: addr=${VOL_ADDR},${VOL_OPTIONS}
      device: ${CONF_VOL_DEVICE:-:/nifi-registry-conf-vol/}

  database-vol:
    name: ${DATABASE_VOL_NAME}
    driver: local
    driver_opts:
      type: ${VOL_TYPE}
      o: addr=${VOL_ADDR},${VOL_OPTIONS}
      device: ${DATABASE_VOL_DEVICE:-:/nifi-registry-database-vol/}

  flow-storage-vol:
    name: ${FLOW_STORAGE_VOL_NAME}
    driver: local
    driver_opts:
      type: ${VOL_TYPE}
      o: addr=${VOL_ADDR},${VOL_OPTIONS}
      device: ${FLOW_STORAGE_VOL_DEVICE:-:/nifi-registry-flow-storage-vol/}

  security-vol:
    name: ${SECURITY_VOL_NAME}
    driver: local
    driver_opts:
      type: ${VOL_TYPE}
      o: addr=${VOL_ADDR},${VOL_OPTIONS}
      device: ${SECURITY_VOL_DEVICE:-:/nifi-registry-security-vol/}
